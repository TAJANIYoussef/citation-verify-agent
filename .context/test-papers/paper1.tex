\documentclass{article}
\begin{document}

The transformer architecture \cite{vaswani2017attention} demonstrates that self-attention
mechanisms can outperform recurrent models on sequence-to-sequence tasks.

BERT \cite{devlin2018bert} improves upon previous language models by using bidirectional
pre-training, achieving state-of-the-art results on multiple NLP benchmarks.

% A claim with no citation that should be flagged
Neural networks show strong performance on image classification tasks without requiring
hand-crafted features.

This method also references a nonexistent paper \cite{broken2099} which cannot be resolved.

\end{document}
