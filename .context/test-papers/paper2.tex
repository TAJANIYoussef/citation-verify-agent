\documentclass{article}
\begin{document}

ResNets \cite{he2016deep} demonstrate that residual connections improve training of very
deep neural networks and achieve state-of-the-art results on ImageNet.

GPT-3 \cite{brown2020language} shows that large language models can perform few-shot
learning across many NLP tasks without fine-tuning.

% Uncited factual claim
Dropout regularization reduces overfitting in deep learning models by randomly
zeroing activations during training.

\end{document}
